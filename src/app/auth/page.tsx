import { http } from '../core'
import UserPut from '../component/UserPut'
interface User{
  userid: number
  name: string,
  password: string,
  email: string,
  created: string
  update: string
  iconUrl?: string
}


export default async function auth(){
  // const httpGet:User[] = await login()


  return(<>
    auth
    
  </>)
}